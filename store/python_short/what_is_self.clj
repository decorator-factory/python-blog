($
  (p
    (mono "self") " is not a keyword " (--) " it's just a convention to use " (mono "self")
    " as the name of the first parameter in methods. A " (it "keyword") " is a reserved word
    that has a special meaning for the parser, like " (mono "if") " or " (mono "import") ". ")
)